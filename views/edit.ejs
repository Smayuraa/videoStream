<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Video</title>
    <style>
        body {
            font-family: 'Helvetica Neue', Arial, sans-serif;
            background-color: #f4f4f4;
            color: #333;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            margin-top: 40px;
            font-size: 2.5rem;
            color: #4a90e2;
        }

        .container {
            width: 50%;
            margin: 50px auto;
            padding: 40px;
            background: #ffffff;
            border-radius: 15px;
            box-shadow: 0px 6px 20px rgba(0, 0, 0, 0.1);
        }

        form {
            display: flex;
            flex-direction: column;
            align-items: center;
        }

        label {
            font-size: 1.2rem;
            margin-bottom: 12px;
            color: #555;
        }

        input[type="text"], input[type="file"] {
            margin: 12px 0;
            padding: 12px;
            background-color: #e0e0e0;
            border: 2px solid #ccc;
            border-radius: 8px;
            color: #333;
            font-size: 1rem;
            width: 100%;
            max-width: 400px;
        }

        input[type="text"]:focus, input[type="file"]:focus {
            border-color: #4a90e2;
            outline: none;
        }

        button {
            padding: 12px 25px;
            background-color: #4a90e2;
            color: #fff;
            font-size: 1.1rem;
            font-weight: bold;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            transition: background-color 0.3s;
            width: 100%;
            max-width: 400px;
        }

        button:hover {
            background-color: #357ab7;
        }

        a {
            display: inline-block;
            margin-top: 30px;
            padding: 12px 30px;
            background: #8e44ad;
            color: #fff;
            text-decoration: none;
            border-radius: 8px;
            font-weight: bold;
            text-align: center;
            width: 100%;
            max-width: 400px;
            transition: background-color 0.3s;
        }

        a:hover {
            background: #7d3c96;
        }
    </style>
</head>
<body>
    <h1>Edit Video</h1>
    <div class="container">
        <form action="/edit/<%= video._id %>" method="POST" enctype="multipart/form-data">
            <label for="name">Video Name:</label>
            <input type="text" name="name" value="<%= video.name %>" required><br><br>
            
            <label for="video">Choose New Video (optional):</label>
            <input type="file" name="video" accept="video/*"><br><br>

            <button type="submit">Update Video</button>
        </form>
        <a href="/show">⬅ Go back to All Videos</a>
    </div>
</body>
</html>
